# Example: Minimal Node.js API

This directory demonstrates how a Node.js REST API service implements the `ai-context-tree` standard. It contains the structure, specification files, configuration rules, and task files representing a mature state of a project.

For details on how the AI agent operates in this project, refer to [AGENTS.md](AGENTS.md).
